# Ethan’s setup

This repository owns Ethan’s interactive personal setup website; the original Agentic Mouse website remains in EthanSK/agentic-mouse.

- Read README.md and SOURCES.md before changing the site. Keep hardware names, dimensions and links tied to verified evidence in docs/gear.json; never publish invoices, order IDs, serial numbers, private screenshots or local settings.
- Only public repositories belong in the Software sidebar; AIMVS is private and must not be linked. Before publication, check every sidebar repository without authentication. The separately published AIMVS dev skill can remain in the skills directory.
- Use frontend-design, write-user-facing-messages, development-workflow and browser-test-on-macbook. Ethan now requires the Codex built-in browser for website tests: do not open, raise or focus personal Chrome. Manually test the finished UI after the last relevant edit, including drag release, wheel direction, actual pinch input when the browser tool supports it, outside-dialog dismissal and every changed primary action; report any unsupported check.
- Agentic Mouse simulator, HUD, mouse models and app icon remain owned by EthanSK/agentic-mouse. Refresh through scripts/sync-sources.py; never invent a second button map or change the installed native application for a website task.
- The interactive Codex desktop is maintained at EthanSK/response-preferences. Inspect its public website and source before changing desktop integration; reuse its public demo and canonical app metadata instead of maintaining competing copies. Never publish raw screenshots containing private conversations.
- Keep the room photograph proportional and preserve Ethan’s face, shorts and deliberately requested sausage legs. Do not add chair-mounted mouse pads or floating desks. Screen-capture requests apply to the monitors in the main room image, not just their click-open demos: inspect both photographed screens at close zoom before publication, and keep private messages out of the screen inserts.
- Use “Ethan’s setup”, “Scroll to zoom” and “Pinch to zoom”. The header’s “GitHub” button links to https://github.com/EthanSK, replacing “Boring website”; do not point it at the setup repository or Agentic Mouse website. Keep headings natural and concise, including “Review code without moving your hand” and “Great for Agentic Engineers”.
- Keep the AI wording disclaimer once, at the bottom of Everything I use; never repeat it inside product, mouse or software dialogs. Before publication, verify the main page contains one copy in that footer.
- Website changes include Pages publication, a successful deployment check and verification of the exact live cache-versioned assets. After publication, leave the built-in browser on the verified public URL; a cached localhost room can outlive its preview server and leave lazy-loaded models unavailable. Check an uncached model request before handing off any local preview. Keep native apps, live OBS recording, unrelated worktrees and user browser windows untouched.

- Never combine separate items into one hotspot, such as Samsung / OBS: use separate nearby labels, with hardware opening its product and software opening its own view.
